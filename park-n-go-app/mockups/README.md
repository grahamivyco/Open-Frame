# Mockups

Auto-generated from `../index.html` with headless Chromium. Two sets:

- **`screen-*.png`** — clean, full-bleed phone screens (390×844 @3x). Drop these into your own
  device frames or tilted scenes.
- **`device-*.png`** — the same screens inside a phone frame on a soft studio backdrop, ready
  to place directly in the case study.
- **`contact-sheet.png`** — a grid overview of every screen.

Regenerate any time by opening the app at `?bare&nodev#<screen>` and screenshotting at
390×844, or re-running the capture script.
