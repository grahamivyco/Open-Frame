# CLAUDE.md — Open Frame portfolio

Context for Claude Code working in this repo. Keep this file current: when you make a meaningful change, update the relevant section below.

## What this is
The portfolio website for **Open Frame**, the design studio of **Graham Ivy** (Minneapolis) — brand & product design: identity, websites, marketing. Tagline: *"Bring your brand into frame."*
Live at **https://openframe.design**.

## Repo, hosting & deploy
- Public repo: **github.com/grahamivyco/Open-Frame**, served by **GitHub Pages** (Settings → Pages → Deploy from a branch: `main` / root).
- **To publish: commit to `main` and push.** The site auto-deploys in ~1–2 minutes.
  ```bash
  git add -A && git commit -m "describe change" && git push
  ```
- Custom domain **openframe.design** (the `CNAME` file). DNS is at **Spaceship**: A-records → GitHub Pages IPs (185.199.108–111.153), `www` CNAME → grahamivyco.github.io.
- Email runs through **Google Workspace** (MX + a google-site-verification TXT at Spaceship). **Do not touch MX/TXT records** — they carry email, not the website.
- `.nojekyll` is present (serve files as-is, no Jekyll). **`404.html` is the SPA fallback — never delete it.**

## File structure
- **`index.html`** — the ENTIRE site. It's a single-file SPA; all CSS is in the `<style>` block and all JS is in the `<script>` block at the bottom. Edit this file directly.
- **`images/`** — assets, referenced as `images/...`: project covers (`cover-*.png`), portrait cut-outs (`graham-*.png`), case-study shots (`hs-*`, `ngm-*`, `witw-*`).
- **`404.html`** — GitHub Pages SPA redirect (rafgraph technique) so clean URLs work on refresh/direct hit.
- **`CNAME`**, **`Graham-Ivy-Resume.pdf`**, **`.nojekyll`**.

## Icons (single library)
- All functional icons come from **one inline Lucide line-icon sprite** defined once at the top of `<body>` (`<svg style="position:absolute"><defs><symbol id="i-…">…</symbol>…</defs></svg>`).
- **Use an icon with `<svg class="…"><use href="#i-…"/></svg>`** — never paste raw icon `<path>`s inline (that's what caused mismatched résumé/mail icons before). To add an icon, add one `<symbol>` to the sprite, then reference it.
- Icons inherit color via `currentColor`: set `color` on the outer `<svg>` (e.g. `style="color:#f24333"`) or via a class. Size via the outer svg's `width`/`height` or CSS (`.mi`, `.ico`, `.pico`, `.bi`, `.ic svg`).
- The brand **spark** (4-point star) and the **open-frame mark** are brand motifs, NOT part of the icon set — leave them as-is.

## How the site works (routing)
- SPA: `const PAGES=[...]`; `show(id)` toggles the visible `.page.active`.
- **Clean path URLs** via the History API. The slug↔id map is `SLUG2ID` in the script. Paths: `/work`, `/services`, `/about`, `/contact`, `/brand-identity-strategy`, `/web-product-design`, `/marketing-content`, `/brand-growth-consulting`, `/park-n-go`, `/hmong-cultural-center`, `/hellospoke`, `/wellness-in-the-woods`, `/needlework-guild-of-mn`.
- Navigation elements use `data-go="<id>"`; a global click handler calls `go(id)` → `history.pushState` + `show`. `404.html` + an inline decoder in `index.html` restore the path on direct/refresh loads.

## Design system (follow this)
- **Colors:** ink `#111110`, ink-2 `#4b4a46`, muted `#84827b`, white, cream panel `#f3f1ea`. Four accents used ONLY as accents/icons, cycled red `#f24333` → blue `#2f6bf0` → yellow `#f4bf3a` → green `#33ac66`.
- **Type:** Fraunces (serif headings), Inter (body/UI), Manrope (wordmark). Loaded from Google Fonts.
- **Radii:** 20px (`--r`), 30px (`--r-lg`), 100px pill. Buttons: **pill = internal nav**, **14px rounded-rect = external/mailto**.
- **Feel:** flat, bright, generous whitespace, **no drop shadows**. Signature motifs: the 4-point "spark" and the open-frame bracket mark. Custom brand cursor — **clickable cards use the custom cursor, not the pointer-hand**.
- Full spec: `Open-Frame-Design-System.md` (kept in Graham's Open Frame folder, not this repo).

## Editing conventions
- One file — edit `index.html`. After JS edits, sanity-check (e.g. extract the last `<script>` and `node --check`). Keep HTML tags balanced.
- Service cards are **white with a 1px outline**. Footer socials are **icon-only, filled**.
- Case-study heroes are two-column responsive **banners** (tag + title + short para + image). The "next case study" block is a **title + big cover-image card**.
- Keep copy tight and on-brand; prefer tasteful/minimal over busy.
- **Device mockups:** website screenshots go in a `.mock` browser-window frame (`.mock-bar` dots + `.url`, `.mock-view` holds the img). Add `scroll` for full-page captures — the window shows a 16/10 top-of-page and, on hover (desktop only), auto-scrolls the whole page; set the scroll distance per image with an inline `--sh:<pct>` (pct = `(1 − 0.625·w/h)·100`). Wrap a mock (or any element) in `.tstage` + class `tilt` for a single Park-n-Go-style angled shot; use `.pscene` with `.phone > .scr > img` for a scene of tilted phones (social/app content). Stages are the **only** place a (soft) shadow is allowed; everything respects `prefers-reduced-motion` and flattens on mobile.

## Current state (Jul 2026)
Recently shipped: clean path URLs + `404.html`; banner case-study heroes; big "next case study" cards; white outlined service cards; removed pointer-hand cursor (custom cursor hides native cursor everywhere on desktop); site email → **hello@openframe.design**. **Unified all functional icons to one inline Lucide sprite (`#i-*` via `<use>`)**; contact icons are square-less line icons that color on hover; the "Schedule a call" card is a whole-card link (no button).
Case-study media: `.cs-figure`/`.cs-hero-media` reserve a min-height with a loading shimmer (cleared via the `of-imgok` class on img `load`) so images no longer collapse to a thin line then jump; images use `decoding="async"` and the page preconnects to `grahamivy.com`/`s.wordpress.com`.
**HelloSpoke, Wellness & Needlework case studies reframed as clean device mockups** (see Editing conventions → Device mockups): tilted homepage/social heroes, `.mock` browser windows for the real page screenshots, and a tilted 3-phone VPSN scene for Wellness. This also **removed the two slow `s.wordpress.com/mshots` hero shots** (they're now local mockups). Self-hosted the HelloSpoke typography + mascot boards into the design-system row.
Added a **favicon** (black open-frame mark on transparent — `favicon.svg` flips to a cream mark on dark browser UIs via `prefers-color-scheme`; `favicon.ico` + 16/32 PNG fallbacks stay black; `apple-touch-icon.png` is white-backed so it stays visible on the iOS home screen) and a **social share card** (`og-image.png`, 1200×630, cream background, serif "Bring your brand into frame", `openframe.design` muted in the bottom-right corner) with full **Open Graph + Twitter `summary_large_image`** meta, `canonical`, and `theme-color`, all pointing at `https://openframe.design/` — so links preview properly on iMessage/Slack/LinkedIn/X.
**SEO:** added **JSON-LD structured data** (`Organization`/`ProfessionalService` + `Person` Graham Ivy + `WebSite`, with `sameAs` → LinkedIn) so Google understands Open Frame is a design studio — this is the main lever to stop it conflating the site with the *openFrame* open-source microscope. Also added `robots.txt`, `sitemap.xml` (home + all clean routes), a web app manifest (`site.webmanifest`) with 192/512 PNG icons, `<meta name="robots">`, author, and a keyword-rich `<title>`/description. Larger icons (`icon-192.png`/`icon-512.png`) + the manifest also help Google pick up the site favicon (it can take days–weeks to replace the default globe in search). **After deploy, submit the site in Google Search Console** (verify via the existing Spaceship DNS TXT) and submit `sitemap.xml` there to speed up indexing.
**Park n Go + Hmong images are now self-hosted** — the 37 `grahamivy.com` figures were downloaded, the heavy embedded-raster SVGs compressed to ~2000px **WebP** (`images/png-*.webp` / `hcc-*.webp`; 86 MB of SVG → 3.9 MB of WebP), every `src` updated, and the `grahamivy.com`/`s.wordpress.com` preconnect hints removed. The site now loads **no third-party case-study images**. (Their hero/hi-fi shots already carry the tilted composition, so they only needed localizing; the Hmong "before" screenshot got a `.mock` browser frame.)
**Brand design-system sections rebuilt** (`.bsys`): each case study shows a minimal card-style system — color swatches (chip + name + hex) and typography specimens rendered in the project's **real** typeface. Verified brand data: HelloSpoke (Navy #003A5D / Emerald #24C676 / Nunito), Park n Go (Ink #050505 / Signal Gold #FFC857 / Lexend + Poppins), Hmong (Ink / Jade #329459 / Fuchsia #FF6388 / Outfit), Wellness (Bark #57463D / Sage #949D79 / Periwinkle #5F6E9C / Cream / **Staatliches + Lato**), Needlework (Deep Indigo #2A3A6E / Cranberry #A43E4F / Golden Thread #D4AF37 / Spruce / Linen / **Merriweather + Open Sans**, from the official NGM Branding Guide in Drive — the site's live /about is the source of truth, the Figma is a draft). Those brand fonts are loaded via the Google Fonts `<link>`.
**Project covers** (`.pj` cards, all grids) pull each project's brand accent onto the corner fold + arrow, and on hover reveal a brand-tinted panel with just a one-line project description + the corner arrow (no title) — driven by the `PROJECTS` map in the script (accent/desc per id; the `name`/`font` fields are retained but unused now the hover wordmark was removed); text auto-contrasts to accent luminance.
**Home Services = full-viewport snap panel** (`.svc-stage`): full-width, **white cards with a `--line` hairline outline** (contact-bento style). **No bullet points** — each card is just a **large, viewport-responsive icon** (`clamp(56px,7vw,116px)` — icon sits top, text sinks to the bottom via `margin:0 0 auto`) + title + short description, wrapped in `.svc-txt`. Desktop/tablet keep the **2×2 grid** (big icons fill the square); **mobile (`≤560px`) switches to a single-column stack of full-width horizontal cards** (icon left, text right), sized so all four + the heading fit one viewport. Landscape/short viewports (`max-height:640`) compress the 2×2. Sized to **fit within one viewport on every screen**; hover still shows the fold tab + arrow. **The growth card keeps its `s4` class for `grid-area:d`, but `.svc-stage .svc-card.s4` overrides the global `.svc-card.s4` row/wrap rules so it renders identically to the other three.** `.svc-card` site-wide is white+outline; `.svc-card` is out of the custom-cursor dark-selector so the cursor stays ink over the white cards. (The **services *page*** bento and the service-detail `.svc-bento3` still use the old bulleted cards — only the home `.svc-stage` was restyled.)
**Scroll-snap:** `html{scroll-snap-type:y proximity}` with `scroll-snap-align:start` on the home hero wrapper (`.hero-sect`) and `.svc-stage`, so the home page swipes hero → services as full-screen snap points. `proximity` (not `mandatory`) means the tall lower sections (Work/About/CTA) still scroll freely; other `.page`s have no snap targets so they're unaffected.
**Cover art unified** — Wellness, HelloSpoke & Needlework covers (`cover-*.png`, 900×1200) were rebuilt to match the Park n Go / Hmong style: a brand-system field colour (Wellness Cream #F8F5E4, HelloSpoke Navy #003A5D, Needlework Deep Indigo #2A3A6E) behind individual **cropped screen cards** — no device frames/outlines, rounded corners, whisper-soft shadow — laid in a **counterclockwise-tilted brick** (offset columns, no overlap) from that project's *updated* designs + brand elements (Lou mascot, needlework illustration). Regenerated headlessly via Playwright (scripts in scratchpad, not committed). Only the final PNGs live in `images/`.
**Section eyebrows are now clickable links** (`a.eyebrow.eb-link` with `data-go="…"`): at rest they read the section label (Work / Services / About / Contact); on hover an up-right arrow **pops in** (opacity + overshoot transform) and the label text **changes colour** — the wording does **NOT** swap (this matches the contact-band eyebrow). The `.eb-b` call-to-action span is globally `display:none` (`.eb-link .eb-b{display:none}`) so only the plain label ever shows; the markup keeps both `.eb-a`/`.eb-b` spans grid-stacked (`.eb-txt>span` share `grid-area:1/1`) but only `.eb-a` renders. *(Earlier the text swapped to "See all work"/"View our services"/etc. on hover; Graham wanted the word to stay put and just get an arrow + colour, like contact.)* The spark keeps its `c-*` accent (its own `color`) independent of the link text colour; on the dark CTA band `.cta-dark .eb-link` is light. **This replaced the old standalone `.link-arrow` "View all work" buttons** — every "Selected work/projects" head-row and the home Work/Services/About sections use the eyebrow-link instead (the `.link-arrow` CSS/JS rules remain but match nothing now). A **Contact** eyebrow-link sits above the CTA band on every page that has that band (home, work, about, services, 4 service-detail pages — never the contact page itself).
**Per-page SEO** — `show(id)` now calls `setMeta(id)`, which updates `document.title`, the meta description, and the `canonical`/`og:url`/`og:title`/`og:description`/`twitter:*` tags from a `PAGE_META` map (one entry per route) to that route's clean URL. So every page/case-study shares a distinct, keyword-rich title + description for search and link previews.
**Park n Go case study rebuilt** into a tighter, structured "Figma-frame" layout (new `.p2-*` classes) modelled on a Claude design template: a dark hero card (tilted phone + faint open-frame watermark), an inline 4-cell **meta bar** (Scope/Role/Tools/Timeline), a **sticky "On this page" rail** + reading column, and a curated Discover→Define→Deliver narrative. Signature pieces are all real markup: inline **SVG** Double-Diamond + user-journey emotion curve, **stat cards + quote cards**, an **interactive feature-tab switcher** (`.p2-feat`, JS swaps phone + copy), and a **bento brand system** (black logo tile w/ cropped app wordmark, colour swatches, Lexend/Poppins specimens, iconography, spacing scale). The four phone screens + wordmark were **cropped from the existing Park n Go boards** into `images/pngx-*.webp` and sit in dark wells (Park n Go is dark-first). Added seven Lucide symbols to the sprite (`#i-search/pin/clock/car/ticket/user/x`) for the app icon set. The other case studies still use the older long-scroll `.cs-*` layout; the old `png-*.webp` boards remain in `images/` (now unused on the page). Page ends with the shared dark CTA band.
**Home hero — 50/50 "brand mark reel" (real material photos)** — the hero is a **two-column grid** (`.hero{grid-template-columns:1fr minmax(0,50%)}`) with **NO container box** (the old cream `--panel` bg/radius/padding were removed). **Left half:** the words + dark "Start a project" pill, `.hero-inner{max-width:560px;justify-self:center}` so the (left-aligned) text block sits **centred in its half**. **Right half:** the `.hero-reel` panel — `width:100%` of the exactly-50% column and **`height:var(--hero-h)` = the full old cream-box height** (`--hero-h` = `calc(100svh - 12px - var(--gutter) - clamp(84px,9vh,102px))`), `border-radius:clamp(24px,3vw,44px)`, faint inset hairline. It **hard-cuts (1s per frame, `INTERVAL`, no fade)** through **14 photoreal 2000² shots of the Open Frame mark** (`images/hero/of-{chrome,ice,gold,rattan,glass,hedge,embroidery,yarn,beads,sandstone,coral,crayon,tile,succulent}.webp` — user-generated from the reference-template workflow, all identical size/placement; `tile`+`succulent` are framed a bit looser than the rest). **Engine — two stacked `<img class="reel-bg">` layers** (object-fit:cover), double-buffered so nothing ever flashes in half-loaded: the **next** image's `src` is set on the *hidden* `<img>` ~1s ahead, and a layer is revealed (`.show`, opacity 1, *no transition* = hard cut) **only after `img.decode()` resolves** for it — so no cream box / partial paint. It **starts on the FIRST image's decode** (not gated on all 14; the rest are warmed with throwaway `Image()`s). *(Earlier CSS-`background-image` layers caused a cream box because decoding an `Image` object doesn't decode the CSS background — `<img>.decode()` does.)* Random no-repeat order. *(A WebGL "crosswarp" shader transition was tried and reverted — the warp wasn't wanted and WebGL's texture Y-flip turned images upside-down.)* All-photo — no procedural finishes, no `.reel-mark` SVG. Respects **`prefers-reduced-motion`** (one static shot) and **pauses** offscreen (IntersectionObserver) / when hidden. On **≤820px** (mobile/tablet) the hero becomes a **full-viewport flex column** (`min-height:var(--hero-h)`): compact centred words on top (**the eyebrow is `display:none`** to save lines/whitespace) and the photo panel **`flex:1`s to fill all remaining space** (`min-height:clamp(260px,42vh,560px)`, no fixed aspect) so it's never a squished little square. **To add a material: drop a square shot into `images/hero/of-NAME.webp` and append `"NAME"` to `IMG`.** *(Lineage: `THREE.Points` particle field → Three.js "living painting" shader → full-bleed behind-text montage → 50/50 procedural reel → this real-photo reel. Three.js was removed along the way.)*

**Home = full-page snap deck + expanding tiles (EVERY size).** The old jumpy `scroll-snap-type:y proximity` was replaced with **full-viewport page-to-page snapping**, home-only: `show()` toggles `html.snap-home`, and under `@media (min-height:600px)` that class sets `scroll-snap-type:y mandatory` with every `#home>section` (and `.site-footer`) becoming a `height:100dvh` (a **definite** height — `min-height` alone let iOS/WebKit collapse `flex:1` children so the work tiles went invisible and about/footer came up short on iPhone; each panel fits one screen so definite height doesn't clip. **`dvh`, not `svh`** — svh is the toolbar-shown height, so once the mobile browser toolbar retracted the panel stayed short and the next section peeked; `dvh` tracks the currently-visible viewport) `scroll-snap-align:start` panel. So home swipes **hero → services → work → about → contact(CTA band) → footer**, each a full screen — **on mobile, tablet AND desktop** (off only on very short/landscape `<600px` viewports and `prefers-reduced-motion`). **`scroll-snap-stop:always`** on each panel so a fast mobile swipe can't skip one (this was why the work panel seemed to "not load" on phones). **`--head`** = `calc(clamp(84px,9vh,102px) + 12px)` — matches the hero container's top offset (menu height + the gap below it), so every full-screen section sits in the same box the hero does. **Fill modes:** `.snaptop` sections (services, work) are **top-aligned** with the tiles `flex:1` to fill + a **bottom margin = `--gutter`**; **about** (`.sect-narrow`), **contact** (`.cta-band`) and **footer** each **fill the hero-sized box** (`padding-top:--head`, `padding-bottom:--gutter`, inner container `flex:1`). About = **full width** (its `.wrap` max-width raised to `--maxw`, like the hero) + **50/50 photo + copy** on ≥821 (photo `object-fit:cover`, copy vertically centred, left-aligned) / **stacked flex-column** on ≤820 (photo `flex:1 1 0` so it never pushes the copy off-screen). The portrait is **absolutely positioned** so its intrinsic height can't inflate the panel past 100svh (that was causing a taller-than-screen section with two snap stops). Contact uses the **same top/bottom padding as about** so the two tiles match in size + offset — its selector MUST be `#home>section.cta-band` (with the id): a plain `.cta-band` loses to the base `#home>section{padding-top:var(--sect)}` (id specificity) and contact starts too high (this was a recurring bug). its content is **vertically centred** (`.cta-dark` needs `display:flex` for `justify-content` to bite). Footer packs content to the **top** (`align-content:start`) with the two columns **bottom-aligned** (`align-items:end`, so Pages sits level with the social icons) — leaving a big gap above the wordmark. Footer only gets 100svh on home. **Mobile hero** fills top-down with a bottom `padding` incl. `env(safe-area-inset-bottom)` so the photo's bottom clears the browser's bottom toolbar. Work `.pj` tiles have a mobile `min-height` safety net (their wordmark is absolute, so with no in-flow content a flex glitch could collapse them to 0). On **touch** (`@media (hover:none)`) the hover-only corner fold/arrow are dropped so a tapped tile doesn't look stuck-active (iOS keeps `:hover` after a tap); the JS `.open` state keeps its own fold/arrow. The open service tile also has extra `padding-bottom` so its bottom-anchored content isn't flush to the edge.
**CTA-band eyebrow** stays light on the dark band: `.cta-dark a.eyebrow.eb-link:hover` (raised specificity) beats `a.eyebrow.eb-link:hover{color:var(--ink)}` which was turning it black on hover; `.eb-b` is hidden there so the spark + "Contact" sit centred (no wide reserved "Get in touch" box).
**Home Services = vertical accordion at every size** (`.svc-bento.svc-acc`): one tile open (big icon + title + description + **bullet list** + corner fold/arrow), the rest collapsed to icon+title bars; the whole thing **fills the screen** (heading at top). **Hover** opens on pointer devices and a **gentle autoplay** (JS `wire()`, 3.6s) cycles on every device — so on mobile it expands + colours as you land on the panel. **Toolkit icons are thinner** (`stroke-width:1.5`) and **black at rest, brand-colour only when open** (`--sccol` per card). **Outline is a constant colour** (no hover/open change or lift). Each card's `.svc-main` holds an **absolutely-positioned icon** + `.svc-txt`, so the icon can **SMOOTHLY glide between the two states** (you can't tween `flex-direction`, so this is done with animated position + padding): **collapsed = icon centred on the LEFT beside the title; open = icon at the top-left, ABOVE the title, and bigger.** The `.ico` transitions `top:50%`+`translateY(-50%)` → `top:0`+`none` and grows `clamp(26px,2.6vw,38px)` → `clamp(54px,5.6vw,92px)` (colouring to `--sccol`); `.svc-txt` transitions `padding-left`(= collapsed icon+gap, title sits right of icon) → `padding-top`(= big open icon + a **generous gap**, title drops below) so the title slides to match. `.svc-main` is `position:relative;display:flex;flex-direction:column;justify-content:center;min-height:clamp(26px,2.6vw,38px)` (the min-height keeps the absolute icon from clipping and centres the title beside it when collapsed). Applies everywhere the accordion is used — home services, the services page, and the About principles — since they share `.svc-main`. The card is a column too, so the collapsed tile centres `.svc-main` and the **open tile drops it to the bottom** (`justify-content:flex-end`). *(Graham's progression: icon beside title → icon stacked above in both states → this animated left→above glide with a bigger open icon + more icon-to-title space.)* The open card grows more (`flex-grow:5`, collapsed padding trimmed) so there's a clear gap above the bottom-anchored content. On mobile the corner fold is **smaller** (content sits at the bottom, clear of the top-right tab). Bullets are dropped only on **short** screens (`≤700px` tall). Cards keep `data-go`.
**Home Selected work = expanding "logo" tiles** (`.pj-grid.pj-strip`): plain **brand-tinted panels** (no photo, no zoom, no outline) — **Hmong pink `#FF6388`, Park n Go yellow `#FFC857`, Wellness green `#33ac66`, HelloSpoke blue `#2f6bf0`** (`TILE` map in the strip JS; drives the 12% bg tint + fold/arrow). **≥821px = horizontal strip** (collapsed = a vertical `.pj-tab` spine; open = a **bottom-LEFT** `.pj-logo` wordmark + one-line description; the `.pj-logo` has a fixed vw-width so the text never reflows while the tile grows/shrinks — it's just clipped). Project titles now use **one uniform typeface (the site serif, `var(--serif)`)**, not each brand's font. **≤820px = vertical accordion** (name bottom-left always visible, description reveals on open). Hover-opens + autoplay `wire()` (3.2s) at all sizes. *(Typographic wordmark "logos" for now — real logo-art can be dropped in later; only `hs-logo.png`, `ngm-logo.png`, `pngx-wordmark.webp` exist so far.)*
**Footer nav** (`.footer-nav a`): a brand **spark pops in on the left** of each page link on hover (`.fspark`, 0→.82em width). **Footer** carries a **supersized `Open Frame` wordmark** (`.footer-big`, Manrope, `clamp(2.6rem,16.3vw,15.2rem)` — stretched near edge-to-edge) pinned bottom-centre and **half cut off** (clipped by the rounded `.footer-bar`, `overflow:hidden`), a soft `#e5e2d7` — it fills the empty space on the full-screen deck footer only (`html.snap-deck` + `min-height:600px`).
**Mobile hero button** no longer stretches full-width: the `@media (max-width:560px)` full-width `.btn` rule was **un-applied to `.hero-actions`** (still applies to about/cta/book/form buttons), so "Start a project" is natural content width, centred.
**Email links** (`a[href^="mailto:"]`): a bare `mailto:` silently does nothing when no default mail app is registered, so a global click handler also **copies the address to the clipboard and shows a `.toast` confirmation** (the `mailto:` still fires for anyone who has a mail client).

**Snap deck generalized `snap-home`→`snap-deck` + the Work page is now a deck too.** The full-page snap system is no longer home-only: `show()` toggles `html.snap-deck` for **home OR work** (`id==="home"||id==="work"`), and every snap rule targets `#home>section` **and** `#work>section` (same `height:100dvh`, `snaptop`, `.cta-band`/`.sect-narrow` fill, footer rules). **The `/work` page was rebuilt to swipe like home:** `<section id="work">` (dropped its old `.sub` scroll class) now holds just two full-screen panels — a `.snaptop` **"Selected work."** panel using the exact **`.pj-grid.pj-strip`** expanding-tile strip from home (all 5 projects: Hmong, Park n Go, Wellness, HelloSpoke, **Needlework** cranberry `#A43E4F`), then a `.cta-band` **contact** panel with the same dark-band treatment as home (eyebrow+spark "Contact", cta-mark, floating shapes, two buttons) but **different copy** — *"Like what you see?" / "I'm open to freelance, collaboration, and full-time roles — let's talk about your next project."* — then the shared full-screen footer. The strip JS was generalized from `#home .pj-strip` to **`document.querySelectorAll(".pj-strip")`** (so home + work both get `.pj-tab`/`.pj-logo` injected + `wire()` autoplay), and `TILE` gained `needlework:"#A43E4F"`. *(Next: roll the same home-style contact-band + footer treatment across the OTHER pages — about, services, contact, the 4 service-detail pages — per Graham's "put across all the other pages" ask.)*
**Services & About pages rebuilt as full-screen snap decks too** (same `snap-deck` system as home/work). `show()` now toggles `snap-deck` for **home / work / services / about**, and every snap rule adds `#services>section` + `#about>section` (kept id-based to beat the base `#…>section{padding-top:var(--sect)}` specificity — same trap as the contact bug). Both pages dropped their `.sub` class (→ `class="page"`) so the `.page.sub` top-padding doesn't fight the deck.
- **Services** = three full-screen panels + footer: the **`.svc-stage.snaptop` accordion** (same expanding tiles as home, but with **deeper per-service copy** — real paragraphs + 3 bullets each; cards still `data-go` to the detail pages), the **`.pj-strip`** "See it in practice." (all 5 projects), and a **`.cta-band`** with its own copy (*"Not sure where to start?"*).
- **About** = four panels + footer: a **hero** (`.sect-narrow` → `.about-panel`, photo + intro paragraph + résumé/call buttons, reusing the home about-panel fill treatment), **Principles** as the **`.svc-stage` accordion** (Human-Centered / Creatively Focused / Data-Driven / Always Evolving — heart/bulb/chart/refresh icons, deeper copy, no `data-go`), **"Where I've worked."** as a **`.pj-strip`** of employer brands, and a **`.cta-band`** (*"Let's make something together."*).
- **Two JS generalizations made this reuse work:** the accordion wiring went from `#home .svc-acc` to **all `.svc-stage .svc-acc`** (so services + about principles autoplay/hover-open too), and the strip wiring now **falls back to `data-name`/`data-desc`/`data-accent`** when a `.pj` isn't in the `PROJECTS` map — that's how the About **career strip** (employers, no case-study page) gets its tab/logo/tint without a PROJECTS entry.
- **Footer** is a single global `<footer class="site-footer">` after all pages, so it already renders on every route; it becomes the **full-screen** panel (giant cut-off wordmark) on the four deck pages (home/work/services/about) and stays the compact footer on the long-scroll pages (contact, service-detail, case studies). *(Still to roll the contact-band + full-screen footer treatment onto contact + the 4 service-detail pages.)*
**Corner-tab (fold/arrow) size is now uniform** across the services accordion and the work/home project strips: both open states use `.fold{width:clamp(80px,15vw,106px);height:…}` (`.svc-stage .svc-acc .svc-card.open .fold` and `.pj-strip .pj.open .fold`) so the tab is the same size at every viewport (services was previously a touch bigger).
**Services ampersand glyph is locked to the active (open) form.** Fraunces is a variable font whose `opsz` (optical-size) axis changes the "&" shape by rendered size, so the collapsed (small) tiles showed a different ampersand than the open (large) one. `.svc-stage .svc-acc .svc-card h3` now sets `font-optical-sizing:none;font-variation-settings:"opsz" 40,"wght" 500` — the glyph shape stays constant while only `font-size` animates, so every card shows the same ampersand.
**The 4 service-detail pages (`svc-brand`/`svc-web`/`svc-marketing`/`svc-growth`) are now full-screen snap decks too** — same `snap-deck` system as home/work/services/about, so each swipes page-to-page through full-screen tiles. `show()` now toggles `snap-deck` for **home / work / services / about / any `svc-*`** (`id.indexOf("svc-")===0`), and every snap rule's selector list gained `#svc-brand>section`…`#svc-growth>section` (kept id-based to beat the base `#…>section{padding-top:var(--sect)}` specificity — same trap as the contact bug). Each page dropped its `.sub` class (`page sub`→`page`). The six panels per page:
- **Hero** — a `.subhero.svc-hero` full-screen tile: 50/50 image + copy on ≥821 (image fills its half at full panel height, copy vertically centred), **stacked** on ≤820 (image `flex:1`s to fill the top, compact copy below). New deck CSS lives beside the about-panel rules; the hero section carries `padding-top:var(--head)` via the same id-scoped padding rule as `.cta-band`.
- **Process** — a **`.svc-stage.svc-acc.svc-acc-num` vertical expanding accordion**: 4 steps — Hello & intro call / Proposal & kickoff / Draft & revisions / Launch & beyond. **No icon and NO corner tab** (fold/arr omitted in markup) — instead each tile shows a serif step **NUMBER** (`.svc-num`, 01–04) that **grows on open** (glides small-left-of-title → big-above-title, colouring to the cycled accent red→blue→yellow→green) to mark the active step, since there's no tab to signal it. `.svc-acc-num` overrides (via the `.svc-acc.svc-acc-num` double-class so it out-specifies the base `.ico`/`.svc-txt` rules) swap the icon's width/height animation for `font-size`, and re-calc the `.svc-txt` padding to clear the number. No `data-go` (steps aren't links); autoplay/hover-open via the shared `.svc-stage .svc-acc` wiring.
- **Projects** — a **`.pj-strip` horizontal expanding tile strip** (per-service curated list; brand=hellospoke/wellness/needlework, web=parkngo/hmong/hellospoke/needlework, marketing=wellness/needlework, growth=wellness/hellospoke). Colours/tab/logo auto-injected by the generalized `.pj-strip` JS from the `PROJECTS`/`TILE` maps.
- **FAQ** — a **`.svc-stage.svc-acc.svc-acc-plain` accordion** styled to match the process tiles (just an eyebrow + title, then the tiles). Each tile's **question is the title, the answer reveals on open** — **no icon, no number, no corner tab, and no +/x**; it simply expands on hover (autoplay cycles it too). `.svc-acc-plain` zeroes the `.svc-txt` icon padding in both states. *(This replaced the old two-column `.svc-faq-wrap` + `.svc-faq` +/- accordion, whose CSS/JS were removed.)*
- **Other services** ("Looking for something else?") — a second **`.svc-stage.svc-acc` accordion** of the 3 *other* services (deep copy + 3 bullets each, `data-go` to the detail page).
- **Contact** — the shared **`.cta-band`** dark tile (added to the id-scoped `.cta-band` padding rule), then the global **full-screen deck footer** (giant cut-off wordmark) — both automatic once the page is a `snap-deck` page.
Old `.svc-steps` / `.svc-bento3` (the previous numbered-steps row + bulleted "other services" bento) are now **unused on these pages** (CSS left in place, harmless). Short/landscape viewports (`<600px` tall) fall back to normal flow like the other deck pages.

**Mobile/tablet menu = full-screen CREAM card (home-page style, ≤860px).** Replaced the old translucent glassmorphism dropdown. The open `.nav-links` is a **deck-card-sized cream (`--panel`) panel** — `position:fixed;top:var(--head);left/right/bottom:var(--gutter)` (same box as a hero/deck section: full screen minus the header + the gap below it), `border-radius:var(--r-lg)`, a faint inset hairline. Nav items are **text-only** (the `.mi` line-icons removed) in the **site font** (`var(--sans)`, large `clamp(1.5rem,6.4vw,2.15rem)`, left-aligned, ink-2 → ink on active). The **active page** gets a **brand sparkle** beside its label: each link carries a `.nav-spark` (globally `display:none`; shown only for `.nav-links a.active` in the ≤860 menu; colours cycle red/blue/yellow/green). *(The `show()` active-marking was fixed so `[data-nav=services]` is active on `/services` itself, not only the `svc-*` detail pages, so its sparkle shows.)* The **CTA** is the dark hero-style pill pushed to the bottom (`margin:auto 0 0`, left-aligned) — its selector is `.nav-links a.nav-cta` so it out-specifies `.nav-links a` (which zeroes padding + inflates the font for the big nav text). Under it, **LinkedIn / Email / Résumé are icon-only left-aligned squares** (labels wrapped in `<span>`, hidden). A **white backdrop** (`.nav-scrim`, a body-level `position:fixed;inset:0;background:var(--bg);z-index:100` sibling after `<header>`, shown via `.site-header:has(.nav.open) ~ .nav-scrim`) sits below the header (z-index 120) + card (119) but above the page, so the card's gutters read as clean empty bg (like a deck page) instead of the live page bleeding through the rounded corners; tapping it closes the menu.
Open/optional: real per-project logo files (need the source files); submit `sitemap.xml` in Google Search Console; consider rolling the `.p2-*` case-study layout out to the other projects; roll the deck-style contact-band + full-screen footer onto the contact page (home/work/services/about + the 4 service-detail pages are done).

## Working with Graham
Non-technical by background (learning as he goes) — explain what a change does and why, and prefer safe, reversible steps. He often works on **one page at a time**.
